spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reconciliation_db
    username: postgres
    password: MAE2020
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:

      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    baseline-on-migrate: true

logging:
  level:
    com.bakouan.app.reconciliation: DEBUG
