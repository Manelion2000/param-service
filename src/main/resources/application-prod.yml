spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/reconciliation_db
    username: postgres
    password: MAE2020
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    baseline-on-migrate: true

logging:
  level:
    root: INFO
